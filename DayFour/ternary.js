// Ternary - an operator that takes three arguments.

// Normal if statement
if (x < 10) {
    //"yes"
}
else {
    //"no"
}

// Shorthand
let x = 5;
const result = x < 10 ? "yes" : "no"
// result = "yes" if true
// result = "no" if false
console.log(result);